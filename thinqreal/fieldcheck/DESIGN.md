# ThinQ Real FieldCheck — Field 자동 점검 시스템 설계

> **상태**: 설계 확정 (2026-07-30) · 구현 전
> **주의**: 이 시스템은 기존 예약 관리 웹사이트(`thinqreal.html` / `thinqreal_admin.html` / `ThinQReal_AppScript.gs`)와 **별도의 시스템**이다. 기존 파일은 이 폴더의 작업으로 수정하지 않는 것을 원칙으로 하며, 연동 지점(Apps Script 엔드포인트 추가, 관리자 탭 추가)만 명시적으로 계획에 따라 반영한다.

---

## 1. 배경

ThinQ Real에서는 ThinQ ON(생성형 AI 엔진 탑재 음성 허브) 기반 AI홈 솔루션의 동작성 점검을 매일 수행한다 — 기동어 발화 후 일상 대화, 가전 제어, 자동화 루틴(모드) 실행. 이 동작이 원활하지 않으면 ThinQ Real 시연 장애뿐 아니라 일반 가정 고객의 컴플레인으로 직결된다.

### 계기가 된 장애 사례 (2026-07-27 주간, ATOM TTS 서버 장애)
- **현상**: 월요일 오후부터 ThinQ ON 음성 발화 후 무응답 빈도 증가 → 화요일 아침 ThinQ Real 시연 불가 수준으로 저하 (일반 고객 집에서도 재현)
- **임시 조치**: Microsoft TTS 서버를 사용하는 최신 SW 버전으로 변경하여 시연 진행
- **해소**: 화요일 12시 CTO부문에서 LG TTS 서버 대안 확보 → 정상 수준 복구
- **지시 사항**: 단건 해결에 그치지 말고, 음성을 포함한 ThinQ ON 서비스 운영을 체계적으로 모니터링하고 이슈/장애에 대응하는 운영 프로세스를 즉시 수립할 것

핵심 교훈: 이번 장애의 증상은 "발화 후 답변 없음"이었다. **주기적으로 말을 걸고 답변 유무를 확인하는 것만으로도** 시연 불가 수준이 되기 반나절 전에 자동 감지가 가능했다.

## 2. 목표

ThinQ Real 공간에 상주하는 점검 리그가 사람 대신 ThinQ ON에게 주기적으로 말을 걸고, 응답을 판정하여 기록하고, 이상 시 담당자에게 즉시 알리는 **감지 → 알림 → 판단 → 기록** 루프를 만든다.

- 장애를 사람이 눈치채기 전에 시스템이 먼저 보고한다 (특히 시연 시작 전)
- 응답 지연시간·성공률 데이터를 축적한다 (공간 운영 목적인 "데이터 축적·고도화"와 부합)
- 기존 예약 관리 시스템의 백엔드(Apps Script + Sheets)·대시보드·메일 인프라를 재활용한다

## 3. 전체 구조

```
[점검 리그: 노트북 + (3단계부터) USB 웹캠]  — ThinQ Real 공간 내 상주, 사외망 AP 접속
        │  ① 저장된 음성 파일(WAV) 재생: 기동어 + 점검 문장
        │  ② 내장 마이크로 ThinQ ON 응답 녹음
        │  ③ (3단계) 웹캠으로 가전 제어 전/후 촬영
        ▼
[판정 로직 (리그 내 실행)]
        │  L1: 응답 유무 + 지연시간   L2: STT → 기대 키워드 매칭   L3: 이미지 비교/기기 상태
        ▼
[백엔드]  POST → Apps Script → Sheets `health_checks` 탭  (엔드포인트 주소는 설정 파일로 분리)
        ▼
[출력]  관리자 대시보드 🩺 자동 점검 탭  +  실패 시 담당자 3명 메일 알림 (기존 MailApp 파이프라인)
```

## 4. 장비 구성

| 장비 | 선택 | 비고 |
|------|------|------|
| 점검 리그 본체 | **남는 노트북 1대** (Windows) | 내장 스피커·마이크 사용. 배터리 = 순간 정전 대비 UPS 효과 |
| 스피커/마이크 | 노트북 내장 | ThinQ ON 근처 1~2m 배치, 기동어 인식되는 볼륨을 찾은 뒤 **고정** |
| 카메라 | 3단계에서 USB 웹캠 추가 | 내장 웹캠은 화면 방향만 촬영 가능 → 스피커 방향(ThinQ ON)과 카메라 방향(가전)을 분리하기 위해 별도 웹캠 필요 |
| 네트워크 | ThinQ Real 전용 AP (사외망) | 현 Apps Script 접근에 제약 없음 |

라즈베리파이는 검토 후 보류 — 상시 가동에는 유리하나 운영자에게 리눅스 학습 비용이 발생하고, 노트북이면 로컬 STT(Whisper)까지 한 대로 해결되므로 노트북을 기본으로 한다. 안정화 후 필요 시 이전 가능(코드 재사용).

### 노트북 상시 가동 설정 (구현 시 적용)
- 덮개를 닫아도 절전 모드 진입 안 함 (전원 옵션)
- 전원 연결 시 화면 꺼짐만 허용, 시스템 절전 금지
- 정전 복구 시 자동 부팅(BIOS) + 로그인 시 점검 프로그램 자동 시작
- Windows 업데이트 자동 재부팅 시간대를 점검 외 시간으로 지정

## 5. 판정 3단계

| 단계 | 확인 내용 | 방식 | 주기(안) | 잡을 수 있는 장애 |
|------|----------|------|---------|------------------|
| **L1** | 발화 후 N초 내 음성 응답이 있는가 + 응답 지연시간 | 음성 활동 감지(VAD) — STT 불필요 | 30분~1시간 간격 | **ATOM TTS 장애가 정확히 이 레벨** (무응답) |
| **L2** | 응답 내용이 기대와 맞는가 | 녹음 → STT → 기대 키워드 매칭 | 1일 2~3회 정밀 점검 | 오응답, 엉뚱한 대화, NLU 이상 |
| **L3** | 가전이 실제로 제어됐는가 | 제어 전/후 이미지 비교 (가능 시 ThinQ API 상태 조회 병행) | 정밀 점검에 포함 | 제어 경로 장애, 루틴 미실행 |

원칙: **간단한 체크는 자주, 정밀 체크는 정기적으로.** L1의 무응답/지연 데이터만으로도 이번 장애 클래스는 조기 감지된다.

### 입력 음성의 재현성 (중요)
점검 문장은 매번 TTS로 새로 생성하지 않고, **한 번 생성/녹음한 WAV 파일로 고정**한다. 같은 음성·같은 볼륨·같은 위치에서 재생되어야 "오늘 실패가 ThinQ ON 문제인지, 입력이 달라진 탓인지"를 구분할 수 있다. 스피커 볼륨과 배치는 캘리브레이션 후 변경 금지.

## 6. STT 방침

| 단계 | 방식 | 비고 |
|------|------|------|
| 개인 테스트 (현재) | **방법 A — Google Cloud Speech-to-Text API** (개인 계정) | 월 60분 무료, 초과 시 분당 약 30원. 점검당 녹음 수 초 수준이라 무료 한도 내 운용 가능 |
| 팀 공식 운영 격상 시 | **방법 B — 로컬 Whisper** 전환 또는 보안 검토 후 클라우드 유지 | ThinQ ON 응답 녹음을 외부 클라우드로 보내는 구조는 공식 운영 전 보안 검토 필요 |

코드에서 "녹음 → 텍스트" 부분을 교체 가능한 모듈로 분리하여 전환 비용을 최소화한다. 노트북 성능이면 Whisper 로컬 실행 가능.

## 7. 점검 시나리오

시나리오는 코드에 하드코딩하지 않고 **Sheets에서 편집 가능**하게 한다 (운영자가 코드 수정 없이 문장 추가/변경). 시나리오 항목: `id`, `구분(일상대화/가전제어/루틴)`, `발화 문장(WAV 파일명)`, `기대 키워드`, `판정 단계(L1/L2/L3)`, `L3 촬영 대상`, `사용 여부`.

초기 시나리오(안):
1. **일상 대화 (순수)** — 외부 서비스와 무관한 자유 질문 (L1·L2)
2. **일상 대화 (서비스 연계)** — 날씨 질문으로 AccuWeather 연동 경로 점검 (L1·L2)
3. **가전 제어** — 조명 켜/끄기 (L1·L2·L3: 이미지 밝기 차이로 판정 용이)
4. **자동화 루틴/모드 실행** 1건 (L1·L2·L3)

일상 대화를 두 타입으로 분리하는 이유: 순수 대화만 실패하면 **생성형 엔진 자체** 장애, 날씨만 실패하면 **외부 서비스(AccuWeather) 연동 경로** 장애로 원인을 구분 진단할 수 있다.

## 8. 예약 시스템 연동

- 점검 전 기존 `GET ?type=availability&date=` 조회 → **예약 확정 슬롯 시간대는 점검 자동 스킵** (시연 중 기동어 발화 방지)
- **매일 07:30 전체 정밀 점검** (L1+L2+L3) → 1회차(09:00) 전에 "오늘 시연 가능" 신호 확보
- **각 예약 슬롯 시작 30분 전 L1 스모크 테스트** → 실패 시 즉시 메일 ("N회차 예약 전 점검 실패 — 시연 전 확인 필요")
- 그 외 시간대 30분~1시간 간격 L1 점검

## 9. 백엔드·대시보드·알림

### Sheets `health_checks` 탭 (신설, Apps Script가 첫 호출 시 자동 생성 — `roi_snapshots` 패턴 재사용)
컬럼(안): `id`, `timestamp`, `level(L1/L2/L3)`, `scenario_id`, `result(pass/fail)`, `latency_ms`, `stt_text`, `expected`, `media_ref(녹음/사진 참조)`, `note`

### Apps Script 엔드포인트 추가(안)
- `POST type:health_check` — 점검 결과 저장, `result=fail`이면 담당자 알림 메일 발송
- `GET ?type=health_checks&days=N` — 대시보드용 최근 이력 조회

### 관리자 대시보드 — 🩺 자동 점검 탭 (신설)
- 오늘의 점검 타임라인 (성공/실패/스킵)
- 최근 7일 성공률, 응답 지연시간 추이
- 실패 건 상세: 시나리오, STT 텍스트, 녹음/사진 링크

### 알림
- 실패 시 담당자 3명 + CC(kang.wonseok@lge.com) 메일 — 기존 MailApp 파이프라인·수신자 상수 재사용
- 연속 실패(예: 2회 연속) 시에만 발송하는 옵션으로 오탐 소음 방지 검토

## 10. 사내 이관 대비 설계 원칙 (필수 준수)

기존 예약 관리 시스템이 사내 이관 준비 중이므로, FieldCheck도 함께 이관될 것을 전제로 설계한다.

1. **엔드포인트 주소는 설정 파일로 분리** — 리그 코드는 "설정에 적힌 주소 하나로 결과를 보내는 프로그램". 이관 시 주소 한 줄 교체로 끝나야 한다.
2. **인증은 API 키 방식** — 처음부터 요청에 키를 실어 보내는 구조로 만들어, 이관 시 키만 교체.
3. **미디어(녹음/사진) 저장 위치도 설정으로 분리** — Sheets에는 메타데이터만, 파일은 별도 저장(테스트: 리그 로컬 + 실패 건만 업로드 검토).
4. **데이터 스키마는 단순 행(JSON) 구조 유지** — 어떤 DB로도 매핑 가능하게.
5. **네트워크**: 현재 리그는 사외망 AP라 문제 없음. 이관 후 사내 시스템 접근 가능 여부는 이관 시점에 담당 부서와 확인 (미해결 항목).

## 11. 추진 계획 — Phase 1 / Phase 2

### Phase 1 — 수동 점검 체계 + 시스템 구축·검증 (현재 ~ 동작 검증 완료)
운영 공백 없이 수동 체계를 즉시 가동하고, 그 기간 동안 자동 점검 시스템을 만들어 동작을 검증한다.

- [ ] **수동 일일 점검 즉시 시작** — `DAILY_CHECKLIST.md` 기준, 매일 기록 축적
- [x] **구축 1단계 (최소 버전)**: 노트북에서 WAV 재생 → 응답 녹음 → L1 판정(응답 유무·지연) → Apps Script POST → 실패 시 메일. *구글 클라우드 가입 없이도 시작 가능.*
  - **코드 완성 (2026-07-30)** — `rig/` 폴더 (설치 가이드: `rig/README.md`) + Apps Script `health_check` 엔드포인트. **현장 테스트 대기** (노트북 설치 + Apps Script 재배포 필요)
- [ ] **구축 2단계**: Google STT 연동(개인 계정) → L2 내용 판정 + 점검 시나리오 Sheets 편집화 + 예약 슬롯 회피 스케줄링
- [ ] **구축 3단계**: USB 웹캠 추가 → L3 전/후 촬영 판정
- [ ] **검증**: 수동 점검과 자동 점검을 일정 기간 병행하여 판정 일치율 확인 → 일치 확인 시 Phase 1 종료

### Phase 2 — 정식 운영 격상
검증된 시스템(§3 구성도)을 정식 운영으로 전환한다.

- [ ] 관리자 대시보드 🩺 자동 점검 탭 완성 (타임라인·성공률·지연 추이·실패 상세)
- [ ] 수동 점검을 자동 점검으로 대체 (운영자는 대시보드 확인만)
- [ ] STT 보안 검토 또는 로컬 Whisper 전환 (§6)
- [ ] 사내 이관 시 네트워크·인증 확인 및 엔드포인트 전환 (§10)

## 12. 결정 이력

| 날짜 | 결정 | 사유 |
|------|------|------|
| 2026-07-30 | 리그는 라즈베리파이 대신 **남는 노트북** | 학습 비용 없음, 로컬 Whisper까지 한 대로 해결, 내장 스피커·마이크·배터리 |
| 2026-07-30 | STT는 우선 **개인 Google Cloud 계정**으로 테스트 | 무료 한도 내 운용 가능. 공식 운영 격상 시 보안 검토/Whisper 전환 전제 |
| 2026-07-30 | 코드·문서는 `thinqreal/fieldcheck/`에 격리 | 기존 웹사이트 파일과 혼선 방지 |
| 2026-07-30 | 백엔드는 기존 Apps Script + Sheets 확장 | 인프라 재활용, 이관 대비 원칙(§10)으로 종속성 최소화 |
