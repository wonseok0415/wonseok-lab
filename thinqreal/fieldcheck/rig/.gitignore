# 실제 운영 설정 (api_key 포함) — 예시 파일(config.example.json)만 커밋
config.json

# 리그가 만들어내는 로컬 데이터
recordings/
results.jsonl
state.json

# 점검 음성 파일 (각 리그에서 생성/녹음)
phrases/*.wav

# 자동 실행 로그 + 설치 스크립트가 만드는 실행 파일 (경로가 PC마다 다름)
logs/
schedule/run_once.command
schedule/run_once.bat

# Python 캐시
__pycache__/

# 제어판이 만드는 음성 백업
phrases/backups/
