# 신규 아이디어 트랙 — 진행 기록 (핸드오프 로그)

> 세션 마무리 시 여기에 결정 사항·진행 상황·다음 할 일을 append한다.
> 자동 점검(FieldCheck) 트랙의 기록과 섞지 않는다.

**2026-08-07 — 트랙 개설:**
- 자동 점검 세션에서 분리해 신규 아이디어 논의 전용 트랙 개설 (사용자 요청)
- 경계 확정: ThinQ Real 메인 시스템·FieldCheck 무변경, 순수 추가 방식만
- 아직 논의된 아이디어 없음 — 첫 논의 세션에서 시작

**2026-08-07 (2) — 첫 프로젝트 선정: FieldVoice (현장 목소리 수집·분석) + 설계 v0.1:**
- 후보 5개(디지털 도슨트·시연 준비 체크리스트·방문 여정 메일·교차 분석·환경 로깅) 검토 후,
  **사용자 고유 아이디어 채택**: 도슨트·방문객 2채널 녹음 → 전사 → 인사이트 분석.
  영감: 롱블랙 기사(longblack.co/note/2076 — 자동차 UX 리서치의 질문 설계)
- **질문 설계 원칙 확립** (기사 이식): '왜' 금지(거짓말 유발) / 제품 아닌 삶을 묻기 /
  로망·부러움 질문 / 지켜줌-감시 경계 질문(AI홈에 더 절실) / 의도 밖 자유 발화가 1급 데이터
- **결정 사항**: ① 녹음 동의는 현장 진행(구두+서면 1장, 거부 시 미녹음) — 가동 전
  컴플라이언스 확인 게이트 ② STT는 로컬 Whisper 우선(전사만 담당), 맥락 분석은 LLM 계층,
  화자 분리는 4단 사다리(채널→LLM 라벨링→pyannote→클라우드) ③ 파일럿 장비는 동료 대여
  에어마이크 프로 듀얼 — **채널 분리(L/R vs 믹스) 5분 테스트가 첫 작업** (절차는 DESIGN.md §6)
- **산출물**: `thinqreal/fieldvoice/DESIGN.md` v0.1 (배경/원칙/질문 프레임 v1: 투어 동선×
  5유형 9문항/구조/동의 게이트/장비/로드맵 Phase 0~3/미결정 6건)
- **다음 할 일**: ① 사용자 — 마이크 대여 + 채널 테스트, 컴플라이언스 확인, 명칭 확정
  ② 다음 세션 — 동의서 문안 초안, 질문 프레임 다듬기(기사 전문 확보 시 미이식분 검토)
- 참고: 이 세션은 웹 세션, 작업 브랜치 `claude/thinq-real-new-system-ideas-t044tn`에 푸시
  (main 머지는 사용자 확인 후)
- (후속) PR #23으로 main 머지 완료. 다른 세션과의 파일 공유 이슈는 브랜치 미머지가 원인이었음

**2026-08-07 (3) — 분석 자동화 파이프라인 구축 (`fieldvoice/pipeline/`):**
- 사용자가 1채널 인터뷰 녹음을 확보 → 분석 자동화를 앞당겨 구축 (Phase 2·3의 도구를
  선제 제작 — Phase 1 수동 판독도 이 도구의 단계별 출력으로 수행 가능)
- **구성**: 전사(로컬 faster-whisper, 선택 설치) → LLM 에이전트 4종(화자 라벨링·요약·
  맥락 분석·인사이트 도출, claude-opus-5) → `output/<세션>/` 마크다운 저장.
  웹 UI(Flask, 로컬 전용) + `run.sh` 원클릭 실행 + 샘플 전사(가상 데이터) 동봉
- **1채널 대응**: 화자 라벨링을 전용 에이전트로 — 내용 기반 구분(화자 분리 사다리 2번).
  프롬프트 캐시로 요약·맥락·인사이트 3단계가 전사 전문을 공유 (과금 절감)
- **개인정보**: uploads/·output/·오디오 확장자 전부 .gitignore, README §0에 경고.
  전사 텍스트가 Claude API로 나가는 처리 방식을 동의서에 반영해야 함 (미결정 사항에 연동)
- **검증 상태**: 문법·웹 UI·오류 표면화는 샌드박스에서 확인. **실 LLM 호출·Whisper 전사는
  미검증**(샌드박스에 API 키·모델 다운로드 없음) — 맥북에서 `bash run.sh` 후 "샘플 전사로
  시험" 버튼이 첫 검증
- **다음 할 일**: ① 사용자 — 맥북에서 샘플 시험 → 실제 녹음 분석 ② 30MB 녹음 파일은
  Google Drive 경유로 세션에 전달 (공개 저장소 커밋 금지) ③ 파일럿 결과로 프롬프트 개선

**2026-08-18 — Phase 1 파일럿 완료: 첫 실녹음 전사(맥북) + 수동 판독 (세션에서):**
- **경로 확정 과정**: 세션 컨테이너의 네트워크 정책이 Drive 직접 다운로드·HuggingFace를
  차단(실측 403) → 오디오는 맥북 파이프라인으로 전사, 전사 텍스트만 Drive 경유로 세션 전달.
  claude.ai 환경 설정 UI에서는 네트워크 정책 편집 항목을 찾지 못함(보류)
- **맥북 첫 실행에서 잡은 것**: macOS 기본 python3(3.9) 비호환(3.10+ union 문법) 수정 커밋,
  faster-whisper 설치·전사 정상 동작 확인 (76분 1채널 녹음 → 1,734발화)
- **수동 판독 완료** (판독 리포트는 채팅으로 전달 — 개인정보 포함이라 저장소 밖 유지):
  전사 품질 "요지 파악 가능, 인용은 검수 필요" 판정. 그룹 강의형 시연에는 질문 프레임이
  성립하지 않음 → 운영 프로토콜을 "그룹 시연형/인터뷰형"으로 분리 필요. 도슨트의 구두
  녹음 고지가 이미 존재(서면만 얹으면 동의 절차 완성). 시연 중 실장애 3건이 녹음에 남아
  시연 준비 체크리스트(DemoReady) 아이디어의 실증 근거 확보
- **파이프라인 백로그 (실측 기반)**: ① vocabulary_hint 보강(ThinQ ON 오인식 변형 다수)
  ② 환청 반복 붕괴 필터(동일 발화 수십 회 루프 실측) ③ condition_on_previous_text=False
  시험 ④ medium 모델 비교 ⑤ 그룹 세션 라벨링 정책(개별 구분 시도 금지, 도슨트/방문객 2-way)
- **다음 할 일**: ① 백로그 반영(transcribe.py·agents.py) ② 질문 프레임에 "지금 집에
  하나만 들인다면" 유형 추가 검토(구축 아파트 가설 H1) ③ API 인증 준비되면 같은 전사로
  자동 파이프라인 돌려 수동 판독과 비교(품질 벤치마크) ④ 맥북 main 복귀 리마인드

**2026-08-18 (2) — 세션 마감: 백로그 반영·PR #24 머지 완료:**
- 백로그 4건 코드 반영(환청 압축 필터+셀프테스트 4건·어휘 힌트 실측 보강·
  condition_on_previous_text 설정화·그룹 2-way 라벨링 정책) → **PR #24로 main 머지 완료**.
  맥북은 main 복귀 상태 — 이후 `git pull`만으로 파이프라인 최신 유지
- 원본 음원·전사 파일은 사용자 개인 Drive에 당분간 보관하기로 결정 (파일명에 고객사·실명
  포함 — 링크 공유 금지 유지, 컴플라이언스 확인 후 보관 기한 정리)
- (후속 Q&A) 전사 60분 소요의 원인 정리: 클로바(GPU 서버 팜·한국어 특화 상용) vs
  우리(맥북 CPU·로컬 Whisper) — 프라이버시·비용 0원을 위한 의도적 트레이드오프.
  대응 방향: 로컬 고속화 시험(mlx-whisper 등 Apple GPU 엔진 + beam_size) 우선,
  부족하면 클로바를 컴플라이언스 게이트와 함께 검토 (DESIGN §4 갈림길)
**2026-08-19 — 파일럿 최종: 무과금 백엔드로 실전 완주 + 자동 vs 수동 벤치마크 통과:**
- API 크레딧 과금을 피하기 위해 **Claude Code CLI 백엔드** 추가(PR #25) — 구독 사용량으로
  분석. 사용자가 맥북에서 실녹음 76분을 전사→분석까지 무과금 완주 (약 42분)
- **벤치마크 결론: 자동 분석 신뢰 가능.** 수동 판독과 핵심 일치 + 자동만 잡은 발견 4건
  (녹음 고지→발화 위축 상관, 한계 자인→발화 유발 패턴, 클로징 후 비공식 구간 가치,
  방문객 발화 소실 의심=2채널 마이크 설계의 데이터 근거). 수동 우위는 트랙 맥락 연결
  (DemoReady·FieldCheck 교차)과 일부 자유 발화(자비스 비유·시공 생태계) — 운영 모델은
  "자동 기본 + 사람 리뷰 1회"로 확정
- **반영**: 리포트 헤더 분석 엔진 표기 수정(claude_cli인데 API 모델명 찍히던 버그),
  '검증된 설명 프레임 채집' 섹션을 맥락 분석 프롬프트에 추가(자비스 미채집 보완),
  whisper 기본 모델 small→**medium** (회사 검토 결과 채택, README에 속도 트레이드오프 명기)
- **다음 세션 시작점**: ① 질문 프레임 v2 — 벤치마크가 낳은 개선(그룹/인터뷰형 분기,
  B2B 질문셋, 클로징 후 5분 슬롯, 녹음 고지 1회 원칙)을 DESIGN.md에 반영
  ② 다음 방문 회차에 프레임 실전 투입 준비 ③ (보류) Intel 맥 전사 속도는 medium 채택으로
  당분간 감수 — 불만이면 클라우드 STT 갈림길 재검토

**2026-08-19 (2) — 리포트 1페이지화 + 관리자 연동 설계 + 공용 장비·이관 분석:**
- **리포트 개편 (사용자 요청 — 가독성)**: '리포트 작성(compile)' 에이전트 신설 —
  `report.md`는 고정 구조 1페이지(한 줄 결론/TOP5 표/바꿀 것 3/가설/주의), 상세는
  `report_full.md`로 분리. 샘플 E2E 재검증 (39줄 vs 188줄)
- **관리자 페이지 연동 설계 (DESIGN.md §8 신설)**: voc_reports 탭 + POST type:voc_report
  (FV_API_KEY) + 🎙 현장 인사이트 탭. 원칙: 1페이지 요약만 업로드·수동 게이트 필수·
  GET은 처음부터 관리자 토큰(health_checks 무인증 교훈)·api-contract.md 등재로 이관 계약
  포함. 구현은 실서비스 저장소 순수 추가 — 별도 작업 단위
- **공용 전용 노트북·이관 리스크 분석 (`fieldvoice/OPERATIONS.md` 신설)**: fieldcheck
  DESIGN(§4·8·10) + 실서비스 이관 결정문서(decisions-2026-07-06: EKS 컨테이너·SSO·사내 DB·
  Teams)·api-contract까지 교차 검토. 리스크 7종(R1 자원 경합~R7 이관 정합) 도출.
  핵심 결정 요청 3건: ① 공용 장비에 개인 Claude 계정 상주 금지 → 당분간 "녹음은 공용
  장비/분석은 개인 맥북" 분리, 사내 Claude Code 신청(이관 액션 #8)과 연결 ② 기증 PC OS
  확인(Windows면 run.bat 백로그) ③ 셋업 체크리스트(디스크 암호화 등 5종)는 FieldCheck
  이전 작업과 일괄 — **FieldCheck 트랙과 일정 조율 필요**
- 다음 할 일: 사용자 결정 3건 회신 → 관리자 연동 구현 착수 여부 결정
- (후속) 사용자 회신: 접근은 관리자 3인 한정(R2·R3 하향 반영), 기증 PC는 **Windows**
  (run.bat + claude .cmd 호환 반영), 디스크 암호화 진행 결정. 8/5 리포트 1페이지 개편판
  재출력 전달

**2026-08-19 (3) — 관리자 페이지 연동 구현 완료 (양 저장소, 배포 대기):**
- **실서비스 저장소** 브랜치 `claude/fieldvoice-voc-reports` (순수 추가, 기존 핸들러 무수정):
  ① Apps Script — `POST type:voc_report`(FV_API_KEY, fail-closed, 20KB 초과 거부) +
  `GET ?type=voc_reports`(관리자 토큰 필수) + `voc_reports` 탭 자동 생성
  ② 관리자 페이지 — 분석 섹션 🎙 현장 인사이트 탭 (목록→1페이지 상세, 자체 마크다운
  렌더러·XSS escape — 실리포트로 node 검증 통과)
  ③ 이관 문서 등재 — api-contract GET/POST 행, data-schema 4.8 탭 + FC/FV_API_KEY Property 표
- **이 저장소**: 파이프라인 웹 UI에 [관리자 페이지로 업로드] 버튼 — 방문일·목적·작성자·
  동의 입력 + 가명화 확인창(수동 게이트) → report.md만 전송. config `upload` 블록
- **배포 절차 (사용자)**: ① 양 저장소 브랜치 머지 ② Script Property `FV_API_KEY` 신규 등록
  ③ 에디터에 .gs 최신 반영 → **기존 배포 편집(새 버전)** 재배포 — FieldCheck 트랙
  재배포와 충돌 않게 최신 main 확인 ④ 노트북/맥 config.json에 upload.api_key 입력
  ⑤ 검증: 샘플 분석 → 업로드 → 관리자 🎙 탭 확인
  ② API 인증 → 자동 분석 vs 수동 판독 벤치마크 ③ 질문 프레임 v2(H1~H3 가설 반영)
  ④ 판독 리포트는 8/18 세션 채팅에 전달됨(저장소 밖) — 필요 시 사용자 보관본 참조

**2026-08-20 — 컨센서스 자료 + 질문 프레임 v2 + 플로우차트 + 동의 체계 확정:**
- **컨센서스 미팅 자료 (사용자 요청)**: `fieldvoice/PROGRESS_REPORT.md` 신설 (주간 보고
  원본 — 배경/개요/파일럿 결과/구축 현황/결정 요청 3건) + 2장 pptx 덱
  (`build_consensus_deck.js` → `FieldVoice_추진보고_20260820.pptx`). 결정 요청:
  ① 정식 운영 전환 ② 녹음 동의 방식 승인 ③ 사내 Claude Code 신청(이관 액션 #8 연계)
- **녹음 동의 체계 확정 (사용자 결정)**: 예약 시 개인정보 동의(기존 예약 폼) + 현장 구두
  고지 1회 — **별도 서면 동의서 생략**. 잔여 확인 1건: 예약 폼 동의 문안이 '음성 녹음·분석'을
  포괄하는지 1회 확인 (DESIGN.md §5 갱신)
- **질문 프레임 v2 (DESIGN.md §3)**: 인터뷰형(1~2인)/그룹 시연형(3인+) 프로토콜 분기,
  운영 수칙 6칙(녹음 고지 1회·클로징 후 5분 슬롯·자기개방 후 되묻기 등), B2B 질문셋 4,
  가설 A/B/C 검증 질문 — 다음 방문 회차 실전 투입 대기
- **파이프라인 플로우차트 (DESIGN.md §4, mermaid)**: 녹음→STT→라벨링→요약/맥락→인사이트→
  리포트 편집→수동 게이트→관리자 페이지. LLM 백엔드 교체 가능(구독 CLI/API/로컬 gemma 검토)
  점선 표기. GitHub이 md 안 mermaid를 직접 렌더 — main 머지 후 저장소에서 바로 보임
- **팀장 리뷰용 산출물 소개 HTML**: 8/5 실리포트 실물 + 생산 과정 5단계 + 산출물 패키지
  표를 비공개 웹페이지(Artifact)로 게시해 전달 (저장소 밖 — 세션 산출물)
- 팀원 별도 실험 진행 중: Whisper large 계열 + gemma 로컬 LLM — 결과에 따라 whisper 기본값
  교체·gemma 백엔드 추가 검토
- 다음: 질문 프레임 v2 실전 투입 → 회차별 리포트 축적 시작. Apps Script 재배포(FV_API_KEY)는
  여전히 사용자 대기 항목

**2026-08-21 — FieldVoice 트랙 일시 보류 (FieldCheck 업무 우선, 상태 스냅샷):**
- **저장소 상태**: PR #28 머지 완료 — 컨센서스 자료·질문 프레임 v2·플로우차트(mermaid,
  main에서 렌더 확인)·동의 확정이 전부 main 반영. 이 트랙의 미커밋 작업 없음 (깨끗)
- **산출물 소개 HTML (Artifact, 저장소 밖)**: 팀장 리뷰 피드백 반영해 **독자별 2층
  구조로 개편** — ①-A 리더 요약 리포트(팀장·실장용: 사업 관점 발견 3건+시사점, 신뢰
  근거, '바꿀 것'·'가설' 제외) / ①-B 실무 리포트(담당·도슨트용: 기존 1페이지 그대로).
  링크: https://claude.ai/code/artifact/a2a7e30a-d509-4efd-a85f-2b22cab67294
- **⏸ 보류 중 결정 대기 1건**: 파이프라인 compile 에이전트는 아직 ①-B 형태만 생성 —
  2층 구조(리더 요약 섹션 추가)를 report.md에 실제 반영할지 팀장 반응 본 뒤 결정
  (사용자 "반영해줘" 시 agents.py compile 프롬프트 소량 수정)
- **기존 대기 항목 (변동 없음)**: ① Apps Script 재배포 — FV_API_KEY Script Property 등록
  + 기존 배포 편집(새 버전), FieldCheck 재배포와 최신 main 기준 조율 ② config.json
  upload.api_key 입력 → 샘플 업로드 → 🎙 탭 검증 ③ 예약 폼 동의 문안 '음성 녹음·분석'
  포괄 여부 확인 ④ 팀원 실험(whisper large 계열·gemma) 결과 회신 ⑤ 질문 프레임 v2
  실전 투입 (다음 방문 회차)
- **다른 트랙과의 경계 재확인**: 이 트랙 산출물은 `thinqreal/fieldvoice/`·`thinqreal/ideas/`
  에만 있음 — FieldCheck 세션(`fieldcheck/`·CLAUDE.md 핸드오프 로그)과 파일 겹침 없음.
  공용 노트북 셋업 체크리스트는 FieldCheck 트랙 소관(`fieldcheck/SETUP_SHARED_LAPTOP.md`)
